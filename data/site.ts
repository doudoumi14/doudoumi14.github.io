// The one host Google should index. The build is also published to
// doudoumi14.github.io, so every URL-bearing tag is pinned to this origin
// rather than to whatever host happened to serve the page.
export const SITE_URL = "https://adembrouri.com";
